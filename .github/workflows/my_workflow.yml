name: Test Workflow

on: push

jobs:
  print_job:
    runs-on: ubuntu-latest
    steps:
      - id: print_env
        run: echo ${{ env }}
      - id: print_vars
        run: echo ${{ vars }}
      - id: print_github
        run: echo ${{ github }}
      - id: print_runner
        run: echo ${{ runner }}
